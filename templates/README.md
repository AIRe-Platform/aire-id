# Templates

These are the example email templates that are used in the module.

This implementation stores them in the Azure Table entities.

The templates are named following `{locale}.{templateName}.{ext}` scheme.

The `.html` files contain the email templates as HTML documents, while `.txt` files are for plain text representation.
